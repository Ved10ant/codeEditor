# AWS (used by deploy scripts and CI)
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012
ECR_REPOSITORY=docker-aws-app
ECS_CLUSTER=docker-aws-cluster
ECS_SERVICE=docker-aws-service

# App (optional — baked at build time via Vite if prefixed with VITE_)
# VITE_API_URL=https://api.example.com
